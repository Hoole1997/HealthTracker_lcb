# 血糖功能优化文档

## 功能概述

本次优化实现了两个主要功能：
1. **血糖等级边界值配置** - 为 mg/dL 和 mmol/L 两种单位分别定义独立的边界值
2. **单位记忆功能** - 记住用户选择的血糖单位偏好

## 血糖等级边界值配置

### 双单位独立边界值设计

#### mmol/L 单位边界值

**默认/禁食/餐前/睡前/运动后**
- **低血糖**: < 4.0 mmol/L
- **正常**: 4.0 ~ 5.5 mmol/L
- **偏高**: 5.5 ~ 7.0 mmol/L
- **高血糖**: ≥ 7.0 mmol/L

**餐后1小时**
- **低血糖**: < 4.0 mmol/L
- **正常**: 4.0 ~ 7.8 mmol/L
- **偏高**: 7.8 ~ 8.5 mmol/L
- **高血糖**: ≥ 8.5 mmol/L

**餐后2小时**
- **低血糖**: < 4.0 mmol/L
- **正常**: 4.0 ~ 4.7 mmol/L
- **偏高**: 4.7 ~ 7.0 mmol/L
- **高血糖**: ≥ 7.0 mmol/L

**运动前**
- **低血糖**: < 5.3 mmol/L
- **正常**: 5.3 ~ 5.5 mmol/L
- **偏高**: 5.5 ~ 7.0 mmol/L
- **高血糖**: ≥ 7.0 mmol/L

#### mg/dL 单位边界值（保持原有）

**默认/禁食/餐前/睡前/运动后**
- **低血糖**: < 72.0 mg/dL
- **正常**: 72.0 ~ 99.0 mg/dL
- **偏高**: 99.0 ~ 126.0 mg/dL
- **高血糖**: ≥ 126.0 mg/dL

**餐后1小时**
- **低血糖**: < 72.0 mg/dL
- **正常**: 72.0 ~ 140.5 mg/dL
- **偏高**: 140.5 ~ 153.0 mg/dL
- **高血糖**: ≥ 153.0 mg/dL

**餐后2小时**
- **低血糖**: < 72.0 mg/dL
- **正常**: 72.0 ~ 84.7 mg/dL
- **偏高**: 84.7 ~ 126.0 mg/dL
- **高血糖**: ≥ 126.0 mg/dL

**运动前**
- **低血糖**: < 95.5 mg/dL
- **正常**: 95.5 ~ 99.0 mg/dL
- **偏高**: 99.0 ~ 126.0 mg/dL
- **高血糖**: ≥ 126.0 mg/dL

### 技术实现

**文件**: `BloodSugarStatus.kt`
- 为每个测量状态定义了两套独立的边界值：`defaultMgdlRanges` 和 `defaultMmolRanges`
- mg/dL 边界值保持原有设计不变
- mmol/L 边界值按照医学标准独立定义
- 移除了单位转换逻辑，直接根据当前单位使用对应边界值

## 单位记忆功能

### 功能特性
- 用户手动选择血糖单位后，系统自动保存偏好
- 下次添加血糖记录时默认选中上次选择的单位
- 支持 MMOL_L 和 MG_DL 两种单位
- 使用 MMKV（SpUtils）进行持久化存储

### 技术实现

**文件**: `BloodSugarUnit.kt`
- `savePreferredUnit(unit)` - 保存用户偏好
- `getPreferredUnit()` - 获取用户偏好（默认 MMOL_L）
- `hasPreferredUnit()` - 检查是否已设置偏好
- `clearPreferredUnit()` - 清除偏好设置

**文件**: `BsRecordViewModel.kt`
- `initializeWithDefaults()` - 使用偏好单位初始化
- `switchUnit()` - 切换单位时自动保存偏好

### 使用场景
1. **新用户**: 默认使用 mmol/L 单位
2. **切换单位**: 用户切换单位后，偏好自动保存
3. **下次使用**: 自动加载上次选择的单位

## 代码修改总结

### 修改的文件
1. `BloodSugarStatus.kt` - 重构为双单位独立边界值配置
2. `BloodSugarRangeManager.kt` - 支持分单位的自定义范围管理
3. `BloodSugarUnit.kt` - 添加单位偏好管理
4. `BsRecordViewModel.kt` - 集成单位记忆功能

### 向后兼容性
- mg/dL 边界值完全保持原有设计，不影响现有用户
- 添加了兼容性方法以支持现有代码调用
- 自动迁移现有用户为默认单位偏好
- 不影响现有血糖记录数据

## 测试建议

### 功能测试
1. **边界值测试**: 验证不同测量时机下的血糖等级判断
2. **单位记忆测试**:
   - 切换单位并重启应用
   - 验证偏好单位是否保持
3. **数据转换测试**: 验证 mmol/L 和 mg/dL 之间的转换精度

### 用户体验测试
1. 首次使用默认单位体验
2. 单位切换流畅性
3. 血糖等级显示准确性

## 部署注意事项

- 功能向后兼容，可安全部署
- 建议在发布前进行充分的回归测试
- 用户偏好设置存储在本地，不影响云端同步

---

*优化完成时间: 2025-09-17*
*构建状态: ✅ 通过*